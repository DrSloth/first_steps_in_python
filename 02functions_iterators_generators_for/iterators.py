# Iterators are things that generate multiple values
# Iterators can be used with the next function

r = iter([1,2,3])
print(next(r))
print(next(r))

# An object that can be put into the iter() function is called iterable
