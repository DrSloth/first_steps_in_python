# Variables can store data
a = 10 # Assigns the integer 10 to the variable a
# In Python variables do not have one fixed type
a = "hello" # Assigns the string "hello" to the variable a

print(a) # Prints the value of a which is now "hello"
